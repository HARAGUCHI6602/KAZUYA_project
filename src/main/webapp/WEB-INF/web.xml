<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

  <display-name>ProductManage</display-name>

  <!-- 文字化け対策（JSP側でも pageEncoding="UTF-8" を設定） -->
  <context-param>
    <param-name>defaultCharacterEncoding</param-name>
    <param-value>UTF-8</param-value>
  </context-param>

  <!-- =========================
       AuthFilter（必要になるまで一時停止）
       ========================= -->
  <!--
  <filter>
    <filter-name>AuthFilter</filter-name>
    <filter-class>model.filter.AuthFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>AuthFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>
  -->

  <!-- =========================================
       Login / Logout は @WebServlet に任せるためコメントアウト
       ========================================= -->
  <!--
  <servlet>
    <servlet-name>LoginServlet</servlet-name>
    <servlet-class>model.servlet.LoginServlet</servlet-class>
  </servlet>
  <servlet-mapping>
    <servlet-name>LoginServlet</servlet-name>
    <url-pattern>/login</url-pattern>
  </servlet-mapping>

  <servlet>
    <servlet-name>LogoutServlet</servlet-name>
    <servlet-class>model.servlet.LogoutServlet</servlet-class>
  </servlet>
  <servlet-mapping>
    <servlet-name>LogoutServlet</servlet-name>
    <url-pattern>/logout</url-pattern>
  </servlet-mapping>
  -->

  <!-- トップアクセス時は /login（@WebServlet("/login")）へ -->
  <welcome-file-list>
    <welcome-file>login</welcome-file>
  </welcome-file-list>

</web-app>

